# Liste — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un dodicesimo modulo — liste di voci da spuntare — usabile dal web e, soprattutto, dettabile al bot: «aggiungi latte, pane e caffè alla spesa».

**Architecture:** Due tabelle (`lists`, `list_items`) con chiave esterna composita `(list_id, user_id)`, come ogni altra relazione del progetto. Sopra, la catena già usata da tutti gli altri moduli: repository → service → (Server Action | tool AI). Nessun livello nuovo, nessuna interfaccia esistente modificata.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript strict, Supabase Postgres + RLS, zod 4, vitest.

**Spec:** `docs/superpowers/specs/2026-09-07-liste-design.md`

## Global Constraints

- **L'AI non riceve mai `userId` dagli argomenti**: lo inietta il server. Ogni tool ha `parameters` senza `userId`/`user_id` — c'è un test del registry che lo verifica su tutti.
- **Ogni funzione di repository prende `userId` come primo argomento dopo `db` e filtra su di esso**, anche quando la RLS coprirebbe: Telegram e cron girano con la service role.
- **Chiavi esterne composite** `(id, user_id)`: il database rifiuta di collegare righe di utenti diversi.
- **Italiano** in UI, messaggi e descrizioni dei tool; **inglese** in codice, nomi di tabella e commenti.
- **Denaro e date**: qui non ce ne sono. Gli istanti sono `timestamptz`.
- Ogni task finisce con `npx tsc --noEmit`, `npx eslint` e i test verdi prima del commit.

---

### Task 1: Schema e isolamento

**Files:**
- Create: `supabase/migrations/0016_lists.sql`
- Modify: `src/types/database.ts` (rigenerato)
- Modify: `tests/integration/rls-isolation.test.ts`

**Interfaces:**
- Consumes: niente.
- Produces: tabelle `lists` e `list_items`; tipi `Row<'lists'>`, `Row<'list_items'>`.

- [ ] **Step 1: Scrivi la migration**

```sql
-- =============================================================================
-- 0016 - Liste
--
-- Due tabelle e non una: le voci non sono un campo di testo dentro la lista.
-- Con un blocco unico, spuntare una riga vuol dire riscrivere tutta la lista,
-- e da telefono è il modo di perdere pezzi.
-- =============================================================================

create table lists (
  id            uuid primary key default gen_random_uuid(),
  user_id       uuid not null references auth.users(id) on delete cascade,
  name          text not null,
  -- false = si svuota (la spesa), true = tiene lo storico (i film visti).
  -- Proprietà della lista, non della voce: è la lista ad avere un carattere.
  keeps_history boolean not null default false,
  archived_at   timestamptz,
  position      integer not null default 0,
  created_via   created_via not null default 'web',
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now(),
  -- Una sola "spesa": quando l'AI risolve «aggiungi alla spesa» non devono
  -- esistere due liste fra cui indovinare, né può crearne una seconda per un
  -- errore di battitura.
  constraint lists_name_unique unique (user_id, name),
  constraint lists_id_user_unique unique (id, user_id)
);

create unique index lists_user_name_lower_idx on lists (user_id, lower(name));
create index lists_user_position_idx on lists (user_id, position);

create table list_items (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references auth.users(id) on delete cascade,
  list_id     uuid not null,
  text        text not null,
  -- Quando, non se: costa uguale e conserva un'informazione che un booleano
  -- butta via per sempre.
  checked_at  timestamptz,
  position    integer not null default 0,
  created_via created_via not null default 'web',
  created_at  timestamptz not null default now(),
  updated_at  timestamptz not null default now(),
  -- Composita: il database rifiuta di attaccare la voce di un utente alla
  -- lista di un altro anche quando la RLS è scavalcata dalla service role.
  constraint list_items_list_fk foreign key (list_id, user_id)
    references lists (id, user_id) on delete cascade
);

create index list_items_list_idx on list_items (list_id, position);
create index list_items_user_open_idx on list_items (user_id) where checked_at is null;

create trigger lists_updated_at before update on lists
  for each row execute function set_updated_at();
create trigger list_items_updated_at before update on list_items
  for each row execute function set_updated_at();

alter table lists enable row level security;
alter table list_items enable row level security;

create policy own_rows on lists
  for all using (user_id = auth.uid()) with check (user_id = auth.uid());
create policy own_rows on list_items
  for all using (user_id = auth.uid()) with check (user_id = auth.uid());
```

- [ ] **Step 2: Applica la migration e rigenera i tipi**

Applicare con l'MCP Supabase (`apply_migration`, nome `lists`), poi:

```bash
npx supabase gen types typescript --project-id zeduybnldzxkmdzbotmr > src/types/database.ts
```

Verificare che `lists` e `list_items` compaiano in `src/types/database.ts`.

- [ ] **Step 3: Estendi il test di isolamento**

In `tests/integration/rls-isolation.test.ts`, aggiungere le due tabelle all'elenco delle tabelle con proprietario già coperte, seguendo esattamente la forma usata dalle altre. Poi aggiungere il caso specifico della FK composita, sul modello di quello già presente per task/progetti:

```ts
it('non lascia agganciare una voce alla lista di un altro utente', async () => {
  const { data: list } = await admin
    .from('lists')
    .insert({ user_id: userA.id, name: `Spesa ${Date.now()}` })
    .select('id')
    .single()

  // Anche con la service role, che scavalca la RLS: qui a rifiutare è il
  // vincolo composito, non la policy.
  const { error } = await admin
    .from('list_items')
    .insert({ user_id: userB.id, list_id: list!.id, text: 'Latte' })

  expect(error).not.toBeNull()
})
```

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npx vitest run tests/integration/rls-isolation.test.ts`
Expected: PASS, incluso il caso nuovo.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/0016_lists.sql src/types/database.ts tests/integration/rls-isolation.test.ts
git commit -m "Add the lists schema"
```

---

### Task 2: Validazione, repository e servizio

**Files:**
- Create: `src/lib/validation/list.ts`
- Create: `src/lib/db/repositories/lists.ts`
- Create: `src/lib/services/lists.ts`
- Create: `tests/unit/validation/list.test.ts`
- Create: `tests/integration/list-service.test.ts`

**Interfaces:**
- Consumes: `Row<'lists'>`, `Row<'list_items'>` dal Task 1.
- Produces:
  ```ts
  // validation/list.ts
  export const createListSchema: z.ZodType<{ name: string; keepsHistory: boolean }>
  export const addItemsSchema: z.ZodType<{ listName: string; items: string[] }>

  // db/repositories/lists.ts
  export type ListRow = Row<'lists'>
  export type ListItemRow = Row<'list_items'>
  selectLists(db: Db, userId: string): Promise<ListRow[]>
  selectListByName(db: Db, userId: string, name: string): Promise<ListRow | null>
  selectListById(db: Db, userId: string, id: string): Promise<ListRow | null>
  insertList(db: Db, userId: string, values: OwnedInsert<'lists'>): Promise<ListRow>
  deleteListRow(db: Db, userId: string, id: string): Promise<boolean>
  selectItems(db: Db, userId: string, listId: string): Promise<ListItemRow[]>
  insertItems(db: Db, userId: string, listId: string, texts: string[], via: Enums['created_via']): Promise<ListItemRow[]>
  updateItemRow(db: Db, userId: string, id: string, patch: Update<'list_items'>): Promise<ListItemRow | null>
  deleteCheckedItems(db: Db, userId: string, listId: string): Promise<number>

  // services/lists.ts
  export type ListSummary = ListRow & { total: number; checked: number }
  createList(db, userId, input: unknown, via?): Promise<ListRow>
  listLists(db, userId): Promise<ListSummary[]>
  getList(db, userId, id: string): Promise<{ list: ListRow; items: ListItemRow[] }>
  addItems(db, userId, input: unknown, via?): Promise<{ list: ListRow; createdList: boolean; items: ListItemRow[] }>
  setItemChecked(db, userId, itemId: string, checked: boolean): Promise<ListItemRow>
  clearChecked(db, userId, listId: string): Promise<number>
  deleteList(db, userId, id: string): Promise<void>
  ```

- [ ] **Step 1: Scrivi i test di validazione, che devono fallire**

In `tests/unit/validation/list.test.ts`, seguendo la convenzione già stabilita in `src/lib/validation/common.ts` (stringa vuota = non compilato → errore su un campo obbligatorio, mai 0, mai silenzio):

```ts
import { describe, expect, it } from 'vitest'
import { addItemsSchema, createListSchema } from '@/lib/validation/list'

describe('createListSchema', () => {
  it('rifiuta un nome vuoto', () => {
    expect(createListSchema.safeParse({ name: '  ', keepsHistory: false }).success).toBe(false)
  })

  it('toglie gli spazi ai lati, che sono un errore di battitura e non un nome', () => {
    expect(createListSchema.parse({ name: '  Spesa ', keepsHistory: false }).name).toBe('Spesa')
  })

  it('si svuota, se non è stato detto altrimenti', () => {
    expect(createListSchema.parse({ name: 'Spesa' }).keepsHistory).toBe(false)
  })
})

describe('addItemsSchema', () => {
  it('scarta le righe vuote invece di creare voci senza testo', () => {
    const parsed = addItemsSchema.parse({ listName: 'Spesa', items: ['Latte', '  ', 'Pane'] })
    expect(parsed.items).toEqual(['Latte', 'Pane'])
  })

  it('rifiuta un elenco che dopo la pulizia è vuoto', () => {
    expect(addItemsSchema.safeParse({ listName: 'Spesa', items: ['   '] }).success).toBe(false)
  })
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npx vitest run tests/unit/validation/list.test.ts`
Expected: FAIL — il modulo `@/lib/validation/list` non esiste.

- [ ] **Step 3: Scrivi gli schemi**

`src/lib/validation/list.ts`, usando i costruttori condivisi di `src/lib/validation/common.ts` come fanno `task.ts` e `project.ts`. `keepsHistory` ha `.default(false)`; `items` è un array di stringhe che vengono ripulite e filtrate con `.transform()`, poi `.refine()` per rifiutare l'array vuoto.

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npx vitest run tests/unit/validation/list.test.ts`
Expected: PASS.

- [ ] **Step 5: Scrivi i test del servizio, che devono fallire**

In `tests/integration/list-service.test.ts`, sul modello di `tests/integration/task-service.test.ts` (utente usa-e-getta creato in `beforeAll`, eliminato in `afterAll`, `describe.skipIf(!supabaseConfigured)`):

```ts
it('crea la lista al volo quando non esiste, e lo dice', async () => {
  const outcome = await addItems(admin, user.id, {
    listName: 'Spesa',
    items: ['Latte', 'Pane'],
  })

  expect(outcome.createdList).toBe(true)
  expect(outcome.items).toHaveLength(2)
})

it('riusa la lista esistente invece di crearne una seconda', async () => {
  await addItems(admin, user.id, { listName: 'Spesa', items: ['Latte'] })
  const outcome = await addItems(admin, user.id, { listName: 'spesa', items: ['Caffè'] })

  // Il nome è unico senza distinzione di maiuscole: «spesa» e «Spesa» sono
  // la stessa lista, altrimenti l'AI ne creerebbe una nuova a ogni frase.
  expect(outcome.createdList).toBe(false)

  const liste = await listLists(admin, user.id)
  expect(liste.filter((l) => l.name.toLowerCase() === 'spesa')).toHaveLength(1)
})

it('conta quante voci restano da spuntare', async () => {
  const { list, items } = await addItems(admin, user.id, {
    listName: 'Valigia',
    items: ['Passaporto', 'Caricabatterie'],
  })
  await setItemChecked(admin, user.id, items[0].id, true)

  const summary = (await listLists(admin, user.id)).find((l) => l.id === list.id)
  expect(summary).toMatchObject({ total: 2, checked: 1 })
})

it('registra quando una voce è stata spuntata, e lo dimentica se la riapri', async () => {
  const { items } = await addItems(admin, user.id, { listName: 'Prova', items: ['Una cosa'] })

  const spuntata = await setItemChecked(admin, user.id, items[0].id, true)
  expect(spuntata.checked_at).not.toBeNull()

  const riaperta = await setItemChecked(admin, user.id, items[0].id, false)
  expect(riaperta.checked_at).toBeNull()
})

it('svuota le spuntate di una lista che si svuota, e lascia stare le altre', async () => {
  const { list, items } = await addItems(admin, user.id, {
    listName: 'Da svuotare',
    items: ['Fatta', 'Da fare'],
  })
  await setItemChecked(admin, user.id, items[0].id, true)

  const tolte = await clearChecked(admin, user.id, list.id)

  expect(tolte).toBe(1)
  const { items: rimaste } = await getList(admin, user.id, list.id)
  expect(rimaste.map((i) => i.text)).toEqual(['Da fare'])
})

it('rifiuta di svuotare una lista che tiene lo storico', async () => {
  const list = await createList(admin, user.id, { name: 'Film visti', keepsHistory: true })
  const { items } = await addItems(admin, user.id, { listName: 'Film visti', items: ['Dune'] })
  await setItemChecked(admin, user.id, items[0].id, true)

  // Lo storico è il motivo per cui la lista esiste: toglierlo su richiesta
  // generica sarebbe cancellare il contenuto invece di riordinarlo.
  await expect(clearChecked(admin, user.id, list.id)).rejects.toThrow(ConflictError)
})
```

- [ ] **Step 6: Esegui i test e verifica che falliscano**

Run: `npx vitest run tests/integration/list-service.test.ts`
Expected: FAIL — i moduli non esistono.

- [ ] **Step 7: Scrivi repository e servizio**

`src/lib/db/repositories/lists.ts` sul modello di `src/lib/db/repositories/inbox.ts`: ogni funzione prende `userId` e filtra su di esso, ogni errore passa da `translateDbError`.

`src/lib/services/lists.ts` sul modello di `src/lib/services/inbox.ts`: `parseOrThrow` per la validazione, `NotFoundError` quando una lista o una voce non esiste, `ConflictError` quando si prova a svuotare una lista con `keeps_history`. `listLists` calcola i conteggi in JavaScript su una sola lettura delle voci: con qualche decina di liste una vista aggregata sarebbe più codice per lo stesso risultato.

- [ ] **Step 8: Esegui i test e verifica che passino**

Run: `npx vitest run tests/integration/list-service.test.ts && npx tsc --noEmit && npx eslint`
Expected: PASS, nessun errore di tipo né di lint.

- [ ] **Step 9: Commit**

```bash
git add src/lib/validation/list.ts src/lib/db/repositories/lists.ts src/lib/services/lists.ts tests/unit/validation/list.test.ts tests/integration/list-service.test.ts
git commit -m "Add the lists service"
```

---

### Task 3: Gli strumenti dell'AI

**Files:**
- Create: `src/lib/ai/tools/lists.ts`
- Modify: `src/lib/ai/tools/registry.ts`
- Create: `tests/integration/list-tools.test.ts`

**Interfaces:**
- Consumes: il servizio del Task 2.
- Produces: `addToListTool`, `checkListItemTool`, `showListTool`, `clearCheckedTool`; registry a 32 strumenti.

- [ ] **Step 1: Scrivi i test degli strumenti, che devono fallire**

In `tests/integration/list-tools.test.ts`, con un `ToolContext` costruito a mano come in `tests/integration/search.test.ts`:

```ts
const context = (userId: string) => ({
  db: admin,
  userId,
  channel: 'telegram' as const,
  timezone: 'Europe/Rome',
  now: new Date(),
})

it('aggiunge più voci in un colpo e dice dove le ha messe', async () => {
  const outcome = await addToListTool.execute(context(user.id), {
    listName: 'Spesa',
    items: ['Latte', 'Pane', 'Caffè'],
  })

  expect(outcome.summary).toContain('Spesa')
  expect(outcome.summary).toMatch(/3|tre/i)
})

it('dice esplicitamente quando la lista non esisteva', async () => {
  // Creare un contenitore vuoto non è inventare un dato, ma deve restare
  // visibile: è l'unico modo per accorgersi di un nome capito male.
  const outcome = await addToListTool.execute(context(user.id), {
    listName: 'Regali di Natale',
    items: ['Sciarpa'],
  })

  expect(outcome.summary).toMatch(/creat/i)
})

it('spunta la voce giusta partendo da come la persona la nomina', async () => {
  await addToListTool.execute(context(user.id), { listName: 'Casa', items: ['Comprare il pane'] })

  const outcome = await checkListItemTool.execute(context(user.id), {
    listName: 'Casa',
    item: 'pane',
  })

  expect(outcome.summary).toMatch(/pane/i)
  const items = (await listLists(admin, user.id)).find((l) => l.name === 'Casa')
  expect(items?.checked).toBe(1)
})

it('non spunta niente a caso quando non capisce quale voce', async () => {
  await addToListTool.execute(context(user.id), { listName: 'Ufficio', items: ['Toner'] })

  await expect(
    checkListItemTool.execute(context(user.id), { listName: 'Ufficio', item: 'banane' }),
  ).rejects.toThrow()
})

it('elenca le liste quando non gliene viene nominata una', async () => {
  const outcome = await showListTool.execute(context(user.id), {})
  expect(outcome.summary).toBeTruthy()
})
```

- [ ] **Step 2: Esegui i test e verifica che falliscano**

Run: `npx vitest run tests/integration/list-tools.test.ts`
Expected: FAIL — `@/lib/ai/tools/lists` non esiste.

- [ ] **Step 3: Scrivi gli strumenti**

`src/lib/ai/tools/lists.ts` con `defineTool`, sul modello di `src/lib/ai/tools/life.ts`. Ogni campo dei `parameters` ha un `.describe()` in italiano — c'è un test del registry che fallisce se ne manca uno. Per risolvere il testo della voce, usare `requireMatch` da `src/lib/ai/tools/shared.ts`, lo stesso meccanismo con cui si risolvono categorie e progetti: se non risolve, si chiede.

Descrizioni (scritte per il modello, dicono anche quando **non** usarle):

- `add_to_list` — «Aggiunge una o più voci a una lista di cose da spuntare (la spesa, cosa mettere in valigia, i film da vedere). Se la lista non esiste la crea. Non usarlo per cose da fare con una scadenza: per quelle c'è create_task.»
- `check_list_item` — «Segna come fatta una voce di una lista.»
- `show_list` — «Legge una lista. Senza nome, elenca le liste esistenti e quante voci restano.»
- `clear_checked` — «Toglie le voci già spuntate da una lista, per riusarla dalla prossima volta.»

Registrarli in `src/lib/ai/tools/registry.ts` in un blocco `// Liste`.

- [ ] **Step 4: Esegui i test e verifica che passino**

Run: `npx vitest run tests/integration/list-tools.test.ts tests/unit/ai`
Expected: PASS — inclusi gli invarianti del registry (snake_case, nessun `userId` fra i parametri, ogni campo descritto).

- [ ] **Step 5: Commit**

```bash
git add src/lib/ai/tools/lists.ts src/lib/ai/tools/registry.ts tests/integration/list-tools.test.ts
git commit -m "Teach the assistant about lists"
```

---

### Task 4: La schermata

**Files:**
- Create: `src/app/(app)/lists/page.tsx`
- Create: `src/app/(app)/lists/actions.ts`
- Create: `src/components/lists/list-composer.tsx`
- Create: `src/components/lists/list-card.tsx`
- Create: `src/components/lists/list-items.tsx`
- Modify: `src/lib/navigation.ts`

**Interfaces:**
- Consumes: il servizio del Task 2.
- Produces: rotta `/lists`; voce di navigazione «Liste».

- [ ] **Step 1: Le Server Action**

`src/app/(app)/lists/actions.ts` sul modello di `src/app/(app)/tasks/actions.ts`: ogni azione passa da `runAction`, prende la sessione con `createServerSupabase()` + `requireUserId()`, e chiude con `revalidatePath('/lists')`.

Azioni: `createListAction(formData)`, `addItemAction(listId, text)`, `toggleItemAction(itemId, checked)`, `clearCheckedAction(listId)`, `deleteListAction(listId)`.

- [ ] **Step 2: La pagina e i componenti**

`page.tsx` è un Server Component che legge con `listLists` e `getList`. Una lista sola aperta per volta, scelta con `?lista=<id>`; senza parametro si vede l'elenco.

I componenti seguono le convenzioni già in uso negli altri moduli: `useOptimistic` per la spunta (come in `src/components/tasks/task-row.tsx`), `toast.error` sul fallimento, e **ogni riga espone `data-entity-id`** perché la ricerca globale possa evidenziarla.

Le voci spuntate scivolano in fondo, barrate e in `text-muted-foreground`. Il pulsante «Svuota le spuntate» compare **solo** sulle liste con `keeps_history = false`.

- [ ] **Step 3: La navigazione**

In `src/lib/navigation.ts`, aggiungere al gruppo **Giornata**, dopo Task:

```ts
{ href: '/lists', label: 'Liste', icon: ListChecks },
```

Senza `mobile: true`: la barra del telefono ha quattro posti già occupati e non se ne toglie uno a scatola chiusa.

- [ ] **Step 4: Verifica nel browser**

Avviare l'anteprima, creare un utente usa-e-getta, e provare davvero: creare due liste (una che si svuota, una che tiene lo storico), aggiungere voci, spuntarne una, svuotare. Controllare che il pulsante «Svuota» non compaia sulla lista con lo storico. Cancellare l'utente di prova alla fine.

- [ ] **Step 5: Commit**

```bash
git add src/app/\(app\)/lists src/components/lists src/lib/navigation.ts
git commit -m "Add the lists screen"
```

---

### Task 5: Documentazione e verifica finale

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Aggiorna il README**

Aggiornare il conteggio degli strumenti (28 → 32) nello schema in cima, e aggiungere un paragrafo sulle liste che dica **perché** sono due tabelle e cosa vuol dire `keeps_history`.

- [ ] **Step 2: La verifica completa**

```bash
npm test && npx tsc --noEmit && npx eslint && npm run build
```
Expected: tutto verde.

- [ ] **Step 3: Commit e push**

```bash
git add README.md
git commit -m "Document lists"
git push origin main
```

- [ ] **Step 4: La prova vera**

Dopo il deploy: dal telefono, «aggiungi latte, pane e caffè alla spesa», poi «cosa c'è nella lista della spesa», poi spuntarne una. Controllare che le stesse voci si vedano nella dashboard.
